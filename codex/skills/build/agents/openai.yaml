interface:
  display_name: "Build"
  short_description: "Implement with worktree, TDD, and PR"
  default_prompt: "Use $build to implement the target with worktree, TDD, and a pull request."
policy:
  allow_implicit_invocation: false
