interface:
  display_name: "To Tasks"
  short_description: "Create sub-issues from an approved plan"
  default_prompt: "Use $to-tasks to split this planned issue into sub-issues."
policy:
  allow_implicit_invocation: false
